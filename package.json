{
  "name": "socket.io-workshop-full",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "~4.10.0",
    "socket.io": "~1.1.0"
  }
}
